const { expect, test } = require("@jest/globals");
const { Game, Node } = require('./game');


describe('linked list tests', () => {
    test('splice works', () => {
        // var list = Node.BuildList([1,2,3,4,5,6,7,8]);
        // var chunk = list.splice(3);
        // console.log(chunk.dump());
        // console.log(list.dump());


    })
})